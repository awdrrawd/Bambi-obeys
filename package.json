{
  "name": "bambi-obeys",
  "private": true,
  "version": "1.6.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && concurrently -k -n build,serve \"vite build --watch\" \"vite preview --port 5174 --strictPort\"",
    "build": "vite build",
    "check": "node --check Bambi-Obeys.user.js && node --check loader.local.user.js && node --check src/main.js && node --check src/app.js && node --check src/ui.js && node --check src/hooks.js && node --check src/config.js && node --check src/i18n.js",
    "preview": "vite preview --port 5174 --strictPort"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^7.0.4"
  }
}
